#pragma once

#include "Window/Control.h"

namespace PPEngine {
    namespace Window {
        class ScrollBar : public Control {
        public:
            ScrollBar();
            ~ScrollBar() override;
        };
    }
}
