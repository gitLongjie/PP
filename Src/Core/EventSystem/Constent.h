#pragma once

#include "Core/Constant.h"

namespace PPEngine {
    namespace Core {
        namespace EventSystem {
            enum class EventType : uint32 {
                Unknow = 0,
                MouseMove,
            };
        }
    }
}
